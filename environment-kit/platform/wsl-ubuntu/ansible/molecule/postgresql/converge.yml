---
- name: Converge postgresql scenario
  hosts: molecule
  become: true
  vars_files:
    - ../resources/common-vars.yml
  pre_tasks:
    - name: Prepare Ansible working directories
      include_tasks: ../../tasks/setup_download_dirs.yml
  tasks:
    - name: Apply cache role
      import_role:
        name: cache

    - name: Apply base role
      import_role:
        name: base

    - name: Install Node.js subtask for neonctl dependency
      import_role:
        name: development-runtime
        tasks_from: nodejs

    - name: Apply postgresql role
      import_role:
        name: postgresql
