---
- name: Converge runtime scenario
  hosts: all
  become: true
  vars_files:
    - ../../group_vars/all.yml
    - ../../group_vars/platform/molecule.yml
    - ../../group_vars/profiles/ci.yml
    - ../resources/common-vars.yml
  vars:
    base_system_packages:
      - curl
      - apt-transport-https
      - gnupg
      - ca-certificates
      - lsb-release
      - pkg-config
      - build-essential
  pre_tasks:
    - name: Prepare Ansible working directories
      include_tasks: ../../tasks/setup_download_dirs.yml
  tasks:
    - name: Apply base role
      import_role:
        name: base

    - name: Apply development-runtime role
      import_role:
        name: development-runtime
