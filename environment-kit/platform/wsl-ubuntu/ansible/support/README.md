# Support

このディレクトリは Ansible 本体を補助する資産をまとめる場所です。

- `generate-requirements.yml`
  - `requirements.yml` の生成補助 playbook
- `requirements.yml.j2`
  - `requirements.yml` のテンプレート
- `CUSTOM_SETUP.md`
  - 手動の補助手順

本番の入口は `../playbooks/`、テストは `../molecule/`、背景資料は `../docs/` を参照します。
