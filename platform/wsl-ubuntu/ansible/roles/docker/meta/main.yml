---
# Docker role dependencies
dependencies:
  - role: base

galaxy_info:
  role_name: docker
  author: WSL Ubuntu Environment Setup
  description: Docker container engine and Docker Compose setup
  min_ansible_version: "2.14"
  
  platforms:
    - name: Ubuntu
      versions:
        - jammy  # 22.04 LTS
        - focal  # 20.04 LTS
  
  galaxy_tags:
    - docker
    - containers
    - container-engine
    - docker-compose
    - virtualization